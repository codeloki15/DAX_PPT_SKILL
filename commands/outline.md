---
description: Draft the deck's storyline as action titles only - the argument, before any slide is built
argument-hint: <topic and audience>
---

Draft a storyline for: $ARGUMENTS

Use the `dax-ppt` skill's rules for action titles. Do not build any slides yet.

Produce a numbered list, one line per slide:
- a kicker (section label, 1-3 words)
- the action title: the finding, as a full sentence of 8-18 words, no trailing period
- in brackets, the exhibit that would prove it (chart type, exhibit type, or table) and the data it needs

Then check the list the way a reader would: read only the titles, top to bottom. Do they make one
argument? Is any title a topic label ("Revenue by region") rather than a finding? Rewrite until they do.

Flag every figure the titles depend on that the user has not supplied and no file provides - those
become em-dash placeholders, never estimates. End by asking the user to approve or edit the list.
